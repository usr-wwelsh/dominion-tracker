# Copy this file to .env and fill in values before deploying.
# If .env is absent, docker-compose uses the defaults shown in docker-compose.yml.

# Password for the PostgreSQL database
DB_PASSWORD=changeme

# Host port the app is exposed on (default: 80)
APP_PORT=80
